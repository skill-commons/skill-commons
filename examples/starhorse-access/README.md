# StarHorse migration case

StarHorse is the intended first real positive migration case. This directory deliberately
does not copy the upstream skill body. The Phase 0 migration will import it from the
pinned canonical source only after an AIP namespace and publication-rights assertion is
recorded, and will carry the applicable MIT notice inside the standalone package.
