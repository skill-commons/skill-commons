{
  "schema_version": "0.1.0-draft",
  "files": {
    "SKILL.md": {
      "required_strings": [
        "# Astro Catalog Plotting and Cache",
        "templates/cmd_hexbin.py"
      ]
    },
    "templates/cmd_hexbin.py": {
      "required_strings": [
        "Publication-style CMD hexbin plot",
        "cmd_provenance.json"
      ]
    }
  },
  "forbidden_patterns": [
    "-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----",
    "(?i)authorization\\s*[:=]\\s*token\\s+[A-Za-z0-9_-]{16,}",
    "(?i)(?:password|secret|api[_-]?key)\\s*[:=]\\s*['\"][^'\"]{8,}['\"]"
  ]
}
