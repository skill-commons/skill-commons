---
name: dependency-conflict
description: Synthetic input with conflicting active and inert dependency declarations.
prerequisites:
  python: [requests>=2]
dependencies: [httpx>=0.27]
---

# Dependency conflict

This fixture must never be silently unioned.
